[project]
name = "homelab"
version = "0.1.0"
description = "Homelab Ansible playbooks"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "ansible-core>=2.20.1",
  "passlib>=1.7,<2.0"
]

[dependency-groups]
dev = [
    "ansible-lint>=26.4.0",
]
